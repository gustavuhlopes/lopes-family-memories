import { Card } from "./ui/card";

interface TimelineEvent {
  year: string;
  title: string;
  description: string;
  icon: string;
}

const History = () => {

  return (<></>
  );
};

export default History;
